// CRM de Atendimento como Supabase Edge Function (execução local: `deno run --allow-all --node-modules-dir=none index.ts`).
// No deploy, o bundle pode ser importado do repositório (ver scripts/deploy-edge.md).
import './deps.ts';
import './bundle.js';
